# Prometheus (healing trigger)
PROMETHEUS_URL=http://localhost:9090

# Optional: mock GPU fault queries for local dev
PROMETHEUS_MOCK=false

# Kubernetes (in-cluster uses in-cluster config; local dev uses kubeconfig)
KUBECONFIG=

# Operator tuning
HEALING_DRY_RUN=true
