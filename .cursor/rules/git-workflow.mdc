---
description: Git 分支与提交信息约定
alwaysApply: true
---

# Git 工作流

- **日常开发分支：** `dev`。写代码、改功能都在 `dev` 上进行；开始前确认已检出 `dev`（`git checkout dev`）。
- **`main`：** 稳定/发布用，不直接在 `main` 上开发；合并由用户通过 PR 或手动完成。
- **提交信息：** 使用**中文**，简洁说明「为什么改」，1–2 句即可。

示例：

```
feat: 实现 Node cordon 与污点打标

对接 client-go，为后续自愈驱逐做准备。
```

- 不要擅自 `git push`，除非用户明确要求。
